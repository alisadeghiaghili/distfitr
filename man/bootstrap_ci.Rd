% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_ci}
\alias{bootstrap_ci}
\title{Bootstrap Confidence Intervals for Distribution Parameters}
\usage{
bootstrap_ci(
  fit,
  method = "parametric",
  n_bootstrap = 1000,
  conf_level = 0.95,
  parallel = FALSE,
  n_cores = -1,
  seed = NULL
)
}
\arguments{
\item{fit}{A distfitr_fit object from \code{fit_distribution()}.}

\item{method}{Character. Bootstrap method: "parametric", "nonparametric",
or "bca" (bias-corrected and accelerated). Default is "parametric".}

\item{n_bootstrap}{Integer. Number of bootstrap samples (default: 1000).}

\item{conf_level}{Numeric. Confidence level (default: 0.95).}

\item{parallel}{Logical. Use parallel processing? (default: FALSE)}

\item{n_cores}{Integer. Number of cores to use. -1 for all available cores.
(default: -1)}

\item{seed}{Integer. Random seed for reproducibility (default: NULL).}
}
\value{
An object of class "distfitr_bootstrap" containing:
\item{params}{Original parameter estimates}
\item{bootstrap_samples}{Matrix of bootstrap parameter estimates}
\item{ci}{Confidence intervals for each parameter}
\item{method}{Bootstrap method used}
\item{n_bootstrap}{Number of bootstrap samples}
\item{conf_level}{Confidence level}
}
\description{
Generate bootstrap confidence intervals for fitted distribution parameters.
}
\examples{
# Fit distribution
set.seed(123)
data <- rnorm(100, mean = 5, sd = 2)
fit <- fit_distribution(data, "normal")

# Parametric bootstrap
boot_result <- bootstrap_ci(fit, n_bootstrap = 500)
print(boot_result)

# With parallel processing
boot_result_parallel <- bootstrap_ci(fit, n_bootstrap = 1000, parallel = TRUE)
}
